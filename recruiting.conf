{
  hypnotoad => {
    listen => ['http://*:80']
  },
  sandbox => {
    interpreter => [ qw(/usr/bin/python3 -B -E -I -s) ],
    timeout     => 5 # seconds
  },
  web => {
    interpreter      => './safe-python3',
    max_request_size => 4194304 # 4 MiB
  }
}
